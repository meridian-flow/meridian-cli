Using `meridian-cli` for command-grounding and `review` for the adversarial pass; I’ll read the design package and decisions first, then trace the v2 topology against the reversal points and look for contradictions.
I’ve loaded the review and handoff guidance. Next I’m reading the design package and decisions side by side so I can trace the reversal through every caller/callee boundary instead of reviewing each file in isolation.
I’ve read the full package once. Now I’m doing the adversarial pass: searching for stale v1 language, checking whether the artifact contracts line up across docs, and pressure-testing whether pre-planning actually gives the planner enough runtime truth to solve the original stale-plan problem.
The main seams are clearer now. I’m checking three things before I write findings: whether the plan-review pause is actually executable as specified, whether the redesign loop preserves enough state to restart coherently, and whether the “mandatory structural reviewer” language stays consistent instead of quietly downgrading itself in another doc.
I have the likely findings. I’m doing one last CLI check for the report handoff so I can persist the review through Meridian instead of only returning it inline.
The report write hit the read-only sandbox, so I’m escalating just for the Meridian report creation step. After that I’ll return the findings inline as well.
Overall assessment: the v2 reversal is mostly coherent, and D1/D3/D10/D11 hang together better than v1. But the package still has a few contract gaps on the critical path, so I would treat this as `request changes`, not approved.

- `HIGH` Where: [dev-orchestrator.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/dev-orchestrator.md:21), [impl-orchestrator.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/impl-orchestrator.md:45)
  What: the plan-review pause/resume contract is internally inconsistent. Dev-orch says impl-orch “returns ... and stops” and later execution can happen via “the same spawn or a fresh one.” Impl-orch says it reports the plan and then waits for dev-orch to approve/push back.
  Why it matters: this is the mainline topology after the reversal. One doc assumes a suspended spawn, the other assumes a terminated one; without a single model, implementation will either invent ad hoc resume semantics or accidentally rerun pre-planning/planner and duplicate work.
  Suggested fix: pick one contract and make every doc match it. Either `same spawn waits, then resumes execution`, or `plan review always hands off to a fresh impl-orch with explicit execute-existing-plan semantics and no replanning`.

- `HIGH` Where: [dev-orchestrator.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/dev-orchestrator.md:43), [redesign-brief.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/redesign-brief.md:62), [planner.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/planner.md:74)
  What: the redesign restart path is underspecified. Dev-orch introduces a “preservation hint” and says the next impl cycle starts from the first invalidated phase forward, but no doc defines that artifact, how impl-orch consumes it, or how planner/status artifacts represent preserved phases instead of reseeding everything as `not started`.
  Why it matters: default-preserve is one of the central claims of the redesign loop. As written, the restart path has no concrete data contract, so preserved work can easily be lost, duplicated, or replanned inconsistently.
  Suggested fix: define a concrete preservation input contract and require planner/impl-orch to honor it explicitly in `plan/overview.md` and `plan/status.md`, including how preserved, partially invalidated, and replanned phases are represented.

- `MEDIUM` Where: [design-orchestrator.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/design-orchestrator.md:45), [planner.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/planner.md:57), [impl-orchestrator.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/impl-orchestrator.md:59)
  What: planner is allowed to discover, before execution, that the design is still structurally non-decomposable and “flag it back to impl-orch,” but the rest of the package does not define the corrective path. Impl-orch’s escape hatch is execution-only, while D11 says such a design is not yet converged.
  Why it matters: this leaves the exact failure the restructure is supposed to prevent in a gray zone: a structurally bad design can still slip past planning and into execution, or proceed sequentially, even though the design docs say that should count as non-convergence.
  Suggested fix: add an explicit pre-execution branch. If planner reports “cannot decompose for parallelism because the design preserves coupling,” impl-orch must either bounce the package back to dev-orch/design-orch before execution or record a deliberate sequential-only override with clear criteria.

- `LOW` Where: [feasibility-questions.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/feasibility-questions.md:1), [feasibility-questions.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/feasibility-questions.md:86)
  What: stale v1 wording remains. The intro still frames the skill as something design-orch and impl-orch load, even though the doc later says planner loads it too. It also refers to impl-orch’s “self-planning phase,” which v2 explicitly removed.
  Why it matters: minor, but this is exactly the kind of residue that causes future profile/skill edits to drift back toward the deleted topology.
  Suggested fix: update the intro to name all three loaders and replace “self-planning phase” with the v2 split: `impl-orch pre-planning` plus `planner decomposition`.

Open question: [design-orchestrator.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/design-orchestrator.md:57) says the structural reviewer is “not a separate mandatory pass,” while [design-orchestrator.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/design/design-orchestrator.md:91) and [decisions.md](/home/jimyao/gitrepos/meridian-channel/.meridian/work/orchestrator-restructure/decisions.md:125) make it a hard requirement in fan-out. I read that as “mandatory reviewer within the normal fan-out, not a separate extra gate,” which is coherent, but that sentence could be tightened to avoid misread.

Meridian report was written to [report.md](/home/jimyao/gitrepos/meridian-channel/.meridian/spawns/p1341/report.md).

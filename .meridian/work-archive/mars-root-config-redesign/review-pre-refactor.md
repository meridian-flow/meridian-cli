Repo-root migration is only half-normalized. I did not find many leftover “read config from `.agents/`” mistakes in the touched callers, but the current shape still has a few structural problems that will make the next refactor materially harder. Verdict: request changes before building more config work on top.

**Findings**
- High: `mars init` is no longer idempotent for non-default managed roots. In [`src/cli/init.rs:100`](/home/jimyao/gitrepos/mars-agents/src/cli/init.rs#L100) the command always falls back to `.agents` when `TARGET` is omitted, and in [`src/cli/init.rs:124`](/home/jimyao/gitrepos/mars-agents/src/cli/init.rs#L124) it uses that derived path for `--link` work even when the project was already initialized elsewhere. A project bootstrapped with `mars init .cursor` will get a second empty `.agents/` if someone later runs `mars init --root <project>` or `mars init --link .claude --root <project>`, and subsequent link operations point at the wrong root. This directly violates the new “re-running is a no-op” contract. The fix is to resolve the existing managed root first and only use the default target during first-time bootstrap.
- High: managed-root identity is inferred from filesystem heuristics instead of being persisted authoritatively. [`src/cli/mod.rs:55`](/home/jimyao/gitrepos/mars-agents/src/cli/mod.rs#L55) calls `detect_managed_root()`, and [`src/cli/mod.rs:216`](/home/jimyao/gitrepos/mars-agents/src/cli/mod.rs#L216) picks `.agents` first, otherwise “the only child with `.mars`”, otherwise the first sorted candidate. Meanwhile [`src/config/mod.rs:11`](/home/jimyao/gitrepos/mars-agents/src/config/mod.rs#L11) does not persist the chosen managed target anywhere. That means stale directories or multi-root states are ambiguous by construction, and finding 1 already creates one such state. Before adding symlink/local-package behavior, root selection needs to become data, not discovery heuristics.
- Medium: link persistence is split across three mutation paths, and two of them are currently dead. [`src/sync/mod.rs:62`](/home/jimyao/gitrepos/mars-agents/src/sync/mod.rs#L62) still defines `ConfigMutation::{SetLink,ClearLink}`, [`src/sync/mod.rs:92`](/home/jimyao/gitrepos/mars-agents/src/sync/mod.rs#L92) adds a separate `LinkMutation`, [`src/cli/link.rs:249`](/home/jimyao/gitrepos/mars-agents/src/cli/link.rs#L249) writes `settings.links` directly on link, and [`src/cli/link.rs:523`](/home/jimyao/gitrepos/mars-agents/src/cli/link.rs#L523) uses `mutate_link_config()` only on unlink. This duplicates config-write rules and leaves `ConfigMutation::{SetLink,ClearLink}` plus `LinkMutation::Set` unused. Any schema change in the upcoming refactor will now require auditing multiple codepaths instead of one.
- Medium: `src/config/mod.rs` now conflates consumer config and package manifest semantics in one operational type, which is the wrong seam for the upcoming `[sources]`/`[dependencies]` unification. [`src/config/mod.rs:11`](/home/jimyao/gitrepos/mars-agents/src/config/mod.rs#L11) parses `package`, `dependencies`, `sources`, and `settings` into one `Config`; [`src/config/mod.rs:182`](/home/jimyao/gitrepos/mars-agents/src/config/mod.rs#L182) reparses the same file and projects out a `Manifest`; [`src/config/mod.rs:227`](/home/jimyao/gitrepos/mars-agents/src/config/mod.rs#L227) ignores `package` and `dependencies` entirely when building the effective runtime config. Callers therefore have to know which subset of `Config` is “real” in each context. That ambiguity is manageable today, but it is exactly the coupling that will make the next schema change spread through CLI, resolve, and sync together.

**Verification**
Static review only. I did not run `cargo check` or tests because this session is read-only.

`meridian report create --stdin` is not available in this environment (`Unknown command: report`), so this message is the fallback report.